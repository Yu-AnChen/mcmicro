/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
----------------------------------------------------------------------------------------
*/

process {

    publishDir = [
        path: { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" },
        mode: params.publish_dir_mode,
        saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
    ]

    withName: 'COMPRESS_PYSED' {
        publishDir = []   // storeDir handles persistence; no separate publishDir needed
    }

    withName: 'MULTIQC' {
        ext.args   = { params.multiqc_title ? "--title \"$params.multiqc_title\"" : '' }
        publishDir = [
            path: { "${params.outdir}/multiqc" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: BASICPY {
        tag = { "${meta.id}_${meta.cycle_number}" }
        ext.prefix = { "${meta.id}_${meta.cycle_number}" }
        publishDir = [
            path: { "${params.outdir}/illumination_correction/basicpy" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: ASHLAR {
        ext.args = ''
        publishDir = [
            path: { "${params.outdir}/registration/ashlar" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: 'BACKSUB' {
        ext.prefix = { "${meta.id}_backsub" }
    }

    withName: "DEEPCELL_MESMER" {
        ext.prefix = { "${meta.id}_mask" }
        ext.args = {"--image-mpp=${meta.pixel_size} --nuclear-channel 0 --compartment nuclear"}
        ext.when = {params.stop_after != 'registration' && params.segmentation && params.segmentation.split(',').contains('mesmer')}
        publishDir = [
            path: { "${params.outdir}/segmentation/deepcell_mesmer" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: "CELLPOSE" {
        ext.when = {params.stop_after != 'registration' && params.segmentation && params.segmentation.split(',').contains('cellpose')}
        publishDir = [
            path: { "${params.outdir}/segmentation/cellpose" },
            mode: params.publish_dir_mode,
            saveAs: { filename ->
                if (filename == 'versions.yml') { null }
                else if (filename == "${meta.id}.ome_cp_masks.tif") { "${meta.id}_mask.tif" }
                else { filename }
            }
        ]
    }

    withName: "MCCELLPOSE" {
        ext.when = {params.stop_after != 'registration' && params.segmentation && params.segmentation.split(',').contains('mccellpose')}
        publishDir = [
            path: { "${params.outdir}/segmentation/mccellpose" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: MCQUANT {
        ext.when  = { !['registration', 'segmentation'].contains(params.stop_after) }
        memory    = { 12.GB * task.attempt }
        time      = { 6.h  * task.attempt }
        publishDir = [
            path: { "${params.outdir}/quantification/mcquant/${meta2.segmenter}" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename == 'versions.yml' ? null : filename }
        ]
    }

    withName: "COREOGRAPH" {
        ext.when = { params.stop_after != 'registration' }
        ext.args = { "--downsampleFactor ${Math.round(Math.log(0.65 * 32 / meta.pixel_size) / Math.log(2))}" }
        publishDir = [
            path: { "${params.outdir}/tma_dearray" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]

    }

    withName: "EXTRACT_MARKERS" {
        ext.prefix = { meta.cycle_number != null ? "${meta.id}_${meta.cycle_number}" : "${meta.id}" }
        ext.args   = { params.marker_name_replace ? "--replace '${params.marker_name_replace}'" : '' }
        publishDir = [
            path: { "${params.outdir}/markers" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename == 'versions.yml' ? null : filename }
        ]
    }

    withName: "BFTOOLS_SHOWINF" {
        tag = { "${meta.id}_${meta.cycle_number}" }
        ext.prefix = { "${meta.id}_${meta.cycle_number}" }
        ext.args = { [
            '-no-sas -novalid', // skip StructuredAnnotations and XML validation, for speed
            '-noflat', // don't create explicit Image elements for pyramid subresolutions
            '-option zeissczi.autostitch false -option zeissczi.attachments false', // handle czi images
        ].join(' ').trim() }
    }

    /*
    withName: SCIMAP_MCMICRO {
        containerOptions = '-e NUMBA_CACHE_DIR=/tmp'
    }
    */
}
